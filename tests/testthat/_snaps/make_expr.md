# make_expr

    Code
      make_expr(x)
    Output
      structure(list(x = 1:3), class = "data.frame", row.names = c(NA, 
          -3L))

---

    Code
      make_expr(x)
    Output
      list(1, 3, 4)

